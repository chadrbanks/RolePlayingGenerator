
const fs = require('fs');

module.exports = {

    exec: function (args) {

        console.log("https://www.reddit.com/r/BehindTheTables/wiki/index");
        // Determine Race
        // Determine Gender
        // Determine Class
        // Determine Details
        // Determine Name
    }
};